# Gọi API
requests

# Biến môi trường
python-dotenv

# Database
psycopg2-binary


# Quét YARA (core)
yara-python

# GUI (Windows-first)
customtkinter>=5.2.2

# Hỗ trợ đọc stream OLE (.doc/.xls legacy) khi quét YARA
olefile>=0.47

# Hỗ trợ giải nén ZIP mã hóa AES (MalwareBazaar, VirusShare...)
pyzipper>=0.3.6

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
