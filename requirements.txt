# Gọi API
requests

# Biến môi trường
python-dotenv

# Database
psycopg2-binary

# Xử lý dữ liệu
pandas

# Quét YARA (core)
yara-python

# Hỗ trợ đọc stream OLE (.doc/.xls legacy) khi quét YARA
olefile>=0.47

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
