# Gọi API
requests

# Biến môi trường
python-dotenv

# Database
psycopg2-binary

# Xử lý dữ liệu
pandas

# Quét YARA (core)
yara-python

# Quét archive
py7zr>=0.20.0
rarfile>=4.0

# Hỗ trợ đọc stream OLE (.doc/.xls legacy) khi quét YARA
olefile>=0.47
